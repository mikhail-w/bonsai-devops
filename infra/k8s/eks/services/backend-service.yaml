apiVersion: v1
kind: Service
metadata:
  name: bonsai-backend
  namespace: bonsai
  labels:
    app: bonsai-backend
    tier: backend
spec:
  type: LoadBalancer  
  ports:
    - port: 8000
      targetPort: 8000
      name: http
  selector:
    app: bonsai-backend
    tier: backend