apiVersion: v1
kind: Service
metadata:
  name: bonsai-frontend
  namespace: bonsai
  labels:
    app: bonsai-frontend
    tier: frontend
spec:
  type: NodePort
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30090
      name: http
  selector:
    app: bonsai-frontend
    tier: frontend