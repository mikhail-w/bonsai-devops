apiVersion: v1
kind: Service
metadata:
  name: bonsai-postgres
  namespace: bonsai
  labels:
    app: bonsai
    tier: database
spec:
  ports:
    - port: 5432
      targetPort: 5432
  selector:
    app: bonsai
    tier: database